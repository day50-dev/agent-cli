#!/usr/bin/env python3
"""Zero-install entry point — just run ./agent-cli after cloning."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from agent_cli import main

if __name__ == "__main__":
    main()
